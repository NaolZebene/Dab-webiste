<% layout('client/Boilerplate') %>
<main id="main">

    <section class="breadcrumbs">
        <div class="container">
            <ol>
                <li><a href="/">Home</a></li>
                <li> Events </li>
            </ol>
            <h2>Events</h2>
        </div>
    </section>
    <!-- End Breadcrumbs -->

    <!-- ======= Blog Section ======= -->
    <section id="blog" class="blog">
        <div class="container" data-aos="fade-up">
            <div class="row">
                <div class="col-lg-8 entries">
                    <% if(eventData.length){ %>
                    <% for(let index =0; index<eventData.length; index++){ %>
                    <article class="entry">
                        <div class="entry-img">
                            <img src="/<%=eventData[index].path%>" alt="" class="img-fluid" />
                        </div>

                        <h2 class="entry-title">
                            <a href="/event/<%=eventData[index].eventId%>"><%=eventData[index].title%></a>
                        </h2>

                        <div class="entry-meta">
                            <ul>
                                <li class="d-flex align-items-center">
                                    <i class="bi bi-person"></i>
                                    <a href="#"><%=eventData[index].host%></a>
                                </li>
                                <li class="d-flex align-items-center">
                                    <i class="bi bi-clock"></i>
                                    <a href="#"><time datetime="2020-01-01"><%=eventData[index].eventDate%></time></a>
                                </li>
                            </ul>
                        </div>

                        <div class="entry-content">
                            <p>
                                <%- eventData[index].eventBody.slice(0,340) %> ...
                            </p>
                            <div class="read-more">
                                <a href="/event/<%=eventData[index].eventId %>">Read More</a>
                            </div>
                        </div>
                    </article>
                    <% } %>
                    <% } %>


                </div>
                <!-- End blog entries list -->

                <div class="col-lg-4">
                    <div class="sidebar">
                        <h3 class="sidebar-title">Search</h3>
                        <div class="sidebar-item search-form">
                            <form action="">
                                <input type="text" />
                                <button type="submit"><i class="bi bi-search"></i></button>
                            </form>
                        </div>
                        <!-- End sidebar search formn-->

                        <h3 class="sidebar-title">Categories</h3>
                        <div class="sidebar-item categories">
                            <ul>
                                <% if(categoryData.length){%>
                                <% for(let index = 0; index < categoryData.length; index++){ %>
                                <li>
                                    <a
                                        href="/eventcategory/<%=categoryData[index].title%>"><%=categoryData[index].title%></a>
                                </li>
                                <% } %>
                                <% } %>

                            </ul>
                        </div>
                        <!-- End sidebar categories-->

                        <h3 class="sidebar-title">Recent Posts</h3>
                        <div class="sidebar-item recent-posts">
                            <% if(eventData.length > 3){ %>
                            <% for(let index=3; index > 0; index--){ %>
                            <div class="post-item clearfix">
                                <img src="/<%=eventData[index].path%>" alt="" />
                                <h4>
                                    <a href="/event/<%=eventData[index].eventId%>"><%=eventData[index].title%></a>
                                </h4>
                                <time datetime="2020-01-01"><%=eventData[index].eventDate%>/time>
                            </div>
                            <% } %>
                            <% } %>


                        </div>
                        <!-- End sidebar recent posts-->

                        <h3 class="sidebar-title">Tags</h3>
                        <div class="sidebar-item tags">
                            <ul>
                                <% if(tagData.length){ %>
                                <% for(let index=0; index<tagData.length; index++){ %>
                                <li><a href="/eventtags/<%=tagData[index].title%>"><%=tagData[index].title%></a></li>
                                <% } %>
                                <% } %>
                            </ul>
                        </div>
                        <!-- End sidebar tags-->
                    </div>
                    <!-- End sidebar -->
                </div>
                <!-- End blog sidebar -->
            </div>
        </div>
    </section>
    <!-- End Blog Section -->
</main>
<!-- End #main -->